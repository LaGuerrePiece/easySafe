'use strict';

/**
 * easy-safe-user controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::easy-safe-user.easy-safe-user');
